git clone():add exact copy of remote repository to local repository

git status():to get th list of the intracked files in git 

git add -A: to allow the exact copy of the create file in the git folder to staging process

git commit -m "message":to commit the change to the files in git

git push origin main : to push the files to the remote repository from the local repository